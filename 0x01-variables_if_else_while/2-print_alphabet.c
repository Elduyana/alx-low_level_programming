#include <stdio.h>
/**
* Task 2: a program that will print the alphabet in lowercase
*/
int main(void)
{
int letter;
for (letter = '97'; letter <= '122'; letter++)
{
	putchar(letter);
}
	putchar('\n');
return (0);
}
